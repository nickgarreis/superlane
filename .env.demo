# Demo mode — enables in-memory mock data, no real backend needed
VITE_DEMO_MODE=true
# Dummy values to satisfy any remaining env reads (mocked away at runtime)
VITE_CONVEX_URL=https://demo.convex.cloud
VITE_WORKOS_CLIENT_ID=demo_client_id
VITE_WORKOS_REDIRECT_URI=https://demo.superlane.de/auth/callback
