# #141414 to #101010 Token Update

**Date:** 11-02-2026 11:10

## Summary
Updated editable theme color token values from `#141414` to `#101010`.

## Changes
- Updated `--text-tone-inverse` in `:root` within `src/styles/theme.css`.
- Updated `--text-tone-inverse` in `.dark` within `src/styles/theme.css`.
- Updated `--color-bg-base` in `@theme inline` within `src/styles/theme.css`.

## Validation
- Confirmed no remaining `#141414` occurrences in editable source files under `src/`.
- Confirmed updated token values now use `#101010`.
