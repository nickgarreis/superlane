# Geist Mono to Geist Token Update

**Date:** 11-02-2026 11:12

## Summary
Updated the heading typography token from `Geist Mono` to `Geist`.

## Changes
- Updated Google Fonts import in `src/styles/fonts.css` from `Geist Mono` to `Geist` (weight `700`).
- Updated `--font-family-heading` in `src/styles/theme.css` from `"Geist Mono", "Roboto Mono", monospace` to `"Geist", "Roboto", sans-serif`.

## Validation
- Confirmed no remaining `Geist Mono` references in editable `src/` files.
- Confirmed `--font-family-heading` now resolves to `Geist`.
